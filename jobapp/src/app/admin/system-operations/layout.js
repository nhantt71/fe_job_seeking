export const metadata = {
  title: 'System Operations | Admin Dashboard',
  description: 'Manage system operations and background tasks',
};

export default function SystemOperationsLayout({ children }) {
  return children;
} 